# Task POST ISO examples

- What changed
  - Updated Swagger examples for `POST /tasks` to use ISO date/datetime inputs.
- Why it changed
  - Examples now match supported input formats that are converted to Logseq timestamps.
- New conventions/decisions
  - Task examples use `YYYY-MM-DD` and RFC3339 strings.
- Follow-ups / TODOs
  - None.
