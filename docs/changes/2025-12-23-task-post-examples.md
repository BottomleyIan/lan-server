# Task POST examples

- What changed
  - Updated `POST /tasks` request examples to use today’s date/time format.
- Why it changed
  - Keep Swagger examples aligned with current expectations and avoid stale dates.
- New conventions/decisions
  - Task example timestamps use `YYYY-MM-DD ddd HH:MM` without angle brackets.
- Follow-ups / TODOs
  - Consider generating examples dynamically if we add more date-sensitive docs.
